import React from 'react'
import { View, Text } from 'react-native'

const ProfileScreen = () => {
    return (
        <View style={{ flex: 1 }}>
            <Text style={{ fontSize: 30, justifyContent: 'center', alignItems: 'center' }} > Profile Screen</Text>
        </View>
    )
}

export default ProfileScreen;
